import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    margin: 10,
    fontWeight: "bold",
    fontSize: 30,
    color: "white",
  },
});

export default styles;
